# 文档解析方案调研

> 关联特性：private-chat-attachment-parsing（私聊附件解析）
> 目的：调研业界主流文档解析方案，对比 brain 现状与已提方案的差距，为设计阶段决策提供依据。
> 说明：本文档为调研与建议，不锁定最终方案；耗时类数据需以实测为准。
> 部分外部来源内容为合规改写。

---

## 一、当前主流文档解析方案（2025–2026）

按"自托管纯库 → 自托管面向 LLM → 托管 API → 多模态/VLM"四档排列。

### 第 1 档：纯文本抽取库（自托管，免费）

| 工具 | 特点 | 短板 |
|------|------|------|
| **PyMuPDF** | 业界公认最快的纯 Python PDF 抽取库，benchmark 称 500 页 PDF 数秒内、速度为 pypdf/pdfplumber 数倍 | 整文档一次性处理时需注意内存管理 |
| **pypdf**（brain 现用） | 纯 Python、易用 | 速度与复杂版面处理弱于 PyMuPDF |
| **pdfplumber** | 表格抽取、版面保留最准，内存占用低 | 速度慢 |

> 学术对比 [arXiv 2410.09871](https://arxiv.org/html/2410.09871v1)：文本抽取上 PyMuPDF、pypdfium 普遍优于其他；但所有解析器在科技/专利类复杂文档上都吃力。

### 第 2 档：面向 LLM、直接输出 Markdown（自托管，免费）← 最契合本项目

| 工具 | 特点 |
|------|------|
| **PyMuPDF4LLM** | PyMuPDF 扩展，直接把 PDF 转 GitHub 风格 Markdown（识别标题、粗体、列表、表格、代码块），专为 LLM/RAG 设计 |
| **Docling**（IBM，MIT 开源） | PDF/DOCX/PPTX/HTML/图片 → Markdown，支持 OCR、表格抽取、CPU/GPU、批处理、同步/异步 |
| **MinerU**（开源） | 复杂 PDF/Office → LLM-ready markdown/JSON，面向 agentic 工作流 |

来源：[PyMuPDF4LLM 文档](https://pymupdf4llm.readthedocs.io/)、[docling-api](https://github.com/drmingler/docling-api)、[MinerU](https://github.com/opendatalab/MinerU)。

### 第 3 档：托管解析 API（按页付费，最准）

| 工具 | 特点 |
|------|------|
| **LlamaParse** | 复杂 PDF 准确率高（约 92% F1），多模态 LLM 驱动，约 $0.10/页 |
| **Unstructured.io** | 支持 30+ 文件类型，内置分块 + LangChain 集成，LLM 应用常用默认 |
| **Reducto / AWS Textract / Google Document AI / Azure Document Intelligence / Mistral OCR** | 企业级，强 OCR/版面分析 |

来源：[vstorm 2026 榜单](https://vstorm.co/llamaindex/top-10-document-parsing-services-for-rag-pipelines-and-llm-applications/)、[firecrawl](https://www.firecrawl.dev/blog/best-document-parsing-apis)。

---

## 二、brain 现状 vs 主流方案的差距

> ⚠️ **2026-06 更新（重要纠正）**：本节早期版本称"brain PDF 用 pypdf、输出纯文本"，**该描述已过时且错误**。
> 以实际代码为准（`app/services/parser/`）：
> - **PDF**：`pdf_parser.py` 已用 **PyMuPDF4LLM 输出 Markdown**，文本不足 50 字符时 fallback **PaddleOCR**（扫描件），再不行用 pymupdf 基础文本兜底。
> - **DOCX/XLSX/PPTX**：python-docx / openpyxl / python-pptx。
> - `requirements.txt` 已含 `pymupdf==1.25.3`、`pymupdf4llm==0.0.17`；OCR 依赖在 `requirements-ocr.txt`（可选，约 +1GB）。
>
> 也就是说，下表中"PDF 速度 / 输出格式 / 扫描件"三项在第 1 档方案里**已经按推荐形态落地**。下表保留作历史对比，阅读时请以上述更新为准。

brain 早期现状（已过时，仅作对比）：PDF=pypdf、输出纯文本字符串，不输出 markdown、不做 OCR。

| 维度 | brain 早期（已过时） | 当前实际 | 主流做法 |
|------|-----------|---------|---------|
| 输出格式 | 纯文本，丢结构 | ✅ Markdown（PyMuPDF4LLM） | Markdown |
| PDF 速度 | pypdf（偏慢） | ✅ PyMuPDF（快数倍） | PyMuPDF |
| 扫描件/图片 PDF | 抽不出文字 | ✅ PaddleOCR fallback（可选依赖） | OCR/VLM |
| 表格 | PDF 表格差 | 🟡 PyMuPDF4LLM 保留表格结构，复杂表格仍有限 | pdfplumber/Docling 更强 |
| .doc 老格式 | 用 docx 库硬试，会失败 | 🔴 仍不支持（OLE 二进制） | 需专门处理 |
| **PDF/PPT 内嵌图片** | 不处理 | 🔴 **仍不处理**：抽文字结构，图内信息（图表/示意图/截图文字）丢失；OCR 仅在整页近乎无文字时触发 | VLM 理解 |

**结论**：PDF/Office 文本解析已是"做对"的形态（Markdown + OCR 兜底）。**剩余真缺口集中在两处**：① 旧 `.doc/.xls/.ppt`（OLE）格式不支持；② **文档内嵌图片的语义解析**（待后续，VLM 方向）。

---

## 三、解析能力交付给 agent 的方式（架构对比）

业界两条路线，可组合使用：

- **预处理管线（后端先解析好）**：摄取预处理 → 解析 →（复杂场景上 VLM），见 [Reducto build-vs-buy](https://reducto.ai/blog/build-vs-buy-ai-document-ingestion)。
- **按需工具（agent 自己调）**：
  - **MCP server**：运行时性能、鉴权更强（如 [Foxit 暴露 30+ PDF MCP 工具](https://developer-api.foxit.com/developer-blogs/use-cases-workflow-examples/automated-document-pipelines/foxit-mcp-server-ai-pdf-tools/)）。
  - **Skill / 渐进式披露**（Anthropic Claude Agent Skills）：按需加载，**省上下文、提升工具调用准确率**；[mcpjam 分析](https://www.mcpjam.com/blog/claude-agent-skills) 指出 MCP 在运行时性能与授权上仍有优势。

### 对本项目（openclaw 生态）的具体结论

- 已有 skill 机制（插件同步 SKILL.md），"解析 skill"是顺路的。
- **关键前提**：本地虾跑在用户机器上，**连不到内网 brain**（默认 `localhost:8000`）。走 skill 必须先由 opincer-service 暴露一个**公网、带 API Key 鉴权**的解析接口。
- **可靠性风险**：skill 依赖模型"主动想到去调"，弱模型可能不调 → 仍可能"看不到"。后端注入式不依赖模型聪明程度。

---

## 三种落地方式对比

| 方案 | 谁触发解析 | 优点 | 缺点/前提 |
|------|-----------|------|----------|
| 后端投递前解析 | opincer-service | 虾无需感知；本地/云虾天然一致；不暴露文件给本地环境 | 慢解析需异步；改后端管线 |
| 插件 channel.ts 解析 | 插件 | 不改后端 | 本地虾需能连 brain |
| **Skill / 工具按需调** | 虾自己按需调 | 懒加载、最省上下文、可自行分块、本地云虾同一份 skill | 需后端暴露公网鉴权接口；依赖模型主动调用；需限流防滥用 |

---

## 四、之前所提方案的缺陷（自查）

1. **耗时数字（"几十页一两秒"）是无依据臆测**，已撤回，应以实测为准。（注：早期归因于"pypdf 慢库"——但 brain 实际已是 PyMuPDF4LLM，见第二节更新。）
2. **"截断纯文本注入"丢结构**：markdown 结构被拍平，表格/标题丢失，模型理解力下降。主流是给 markdown。
3. **未考虑扫描件/图片型 PDF**：brain 和原方案对扫描件均会失败，而真实用户文件中很常见。
4. **思路局限在"塞 vs 不塞"**，未把 skill/按需读取作为一等公民——而这是更省上下文、更现代的做法。

---

## 五、综合建议（区分"快速修复"与"做对"）

成熟解法收敛清晰：**面向 LLM 就该输出 Markdown + 按需读取**。

> ⚠️ **2026-06 更新**：本节"中期第 1 条（pypdf → PyMuPDF4LLM）已完成**，brain 现已输出 Markdown + OCR fallback（见第二节更新）。下列保留作历史记录，未完成项已重新归类到第六节。

### 短期（先解决最初的 bug，让本地虾"能看到"）
- 修复插件三处缺陷：`content_type→mime_type`、DM 分支读 `attachments`、纯附件消息不再被丢弃。
- 后端异步调 brain，把解析结果（哪怕纯文本）截断注入消息。

> 注：群聊（`postRoomMessage`）已接 brain 解析注入；**私聊路径（`sendMessage` / `sendConversationMessageJWT`）尚未接**——这是"读文件"主线在私聊场景下仍断的关键缺口，见第六节。

### 中期（做对，建议）
1. ~~**brain 的 PDF 解析从 pypdf 换成 PyMuPDF4LLM**~~ ✅ **已完成**（PyMuPDF4LLM + PaddleOCR fallback）。
2. **解析结果存一份**（仿知识库 `text_content`，存 markdown）；消息给虾**摘要 + 一个"读全文"的 skill/接口**，而非全文硬塞。后端预解析为弱模型兜底（至少有摘要）。
3. ~~扫描件 OCR 作为后续可选项~~ ✅ **已落地**（PaddleOCR，`requirements-ocr.txt` 可选依赖）。

---

## 六、待决策项 / 剩余缺口（留给设计阶段）

> ✅ 已解决：解析库升级（PyMuPDF4LLM）、扫描件 OCR。以下为**仍待处理**项：

1. **私聊路径接入 brain 解析**：`sendMessage` / `sendConversationMessageJWT` 补上与 `postRoomMessage` 一致的附件解析（业务上云虾只跟主人私聊，此路不通则主线在最常用场景失效）。
2. **解析交付形态**：截断注入 / 摘要+按需读取（`read_document`）/ 全文 —— 3.1（后端注入兜底）+ 3.2（云虾按需读）如何组合。
3. **旧格式支持**：`.doc / .xls / .ppt`（OLE 二进制）当前不支持，需 LibreOffice headless 转换。
4. **文档内嵌图片解析**：图表/示意图/截图文字当前丢失，需 VLM 方向（**已确认延后，不在当前主线**）。
5. 解析结果是否落库（仿 `text_content`）、是否与知识库共用。
6. **实测 brain 真实解析耗时**（需可用的 BRAIN_URL）——用真实数据决定是否需要进一步优化。

---

## 参考来源

- [PyMuPDF 生产实践 benchmark](https://markaicode.com/usecases/pymupdf-use-cases-production-workflows/)
- [PDF 解析工具对比研究 arXiv 2410.09871](https://arxiv.org/html/2410.09871v1)
- [PyMuPDF4LLM 文档](https://pymupdf4llm.readthedocs.io/)
- [docling-api](https://github.com/drmingler/docling-api)
- [MinerU](https://github.com/opendatalab/MinerU)
- [2026 文档解析服务榜单 vstorm](https://vstorm.co/llamaindex/top-10-document-parsing-services-for-rag-pipelines-and-llm-applications/)
- [文档解析 API 对比 firecrawl](https://www.firecrawl.dev/blog/best-document-parsing-apis)
- [Reducto build-vs-buy](https://reducto.ai/blog/build-vs-buy-ai-document-ingestion)
- [Foxit MCP PDF 工具](https://developer-api.foxit.com/developer-blogs/use-cases-workflow-examples/automated-document-pipelines/foxit-mcp-server-ai-pdf-tools/)
- [Claude Agent Skills 分析 mcpjam](https://www.mcpjam.com/blog/claude-agent-skills)
